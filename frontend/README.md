# Frontend

React client application and user interface code for the hackathon project.

## Run

```bash
npm install
npm run dev
```

The app starts at `http://localhost:5173`.

Set `VITE_ENABLE_MOCK_VIDEO_ID=true` to force the built-in mock video result during local testing. By default the UI uses parsed agent results.
